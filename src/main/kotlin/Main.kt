import algoexpert.sortedSquaredArray
import algoexpert.twoNumberSum
import constructors.Basic
import constructors.Hui
import constructors.Singleton

fun main(args: Array<String>) {
    val list = mutableListOf(-4,-3,-2,-1,0,1,2,3,4)
    val result = sortedSquaredArray(list)
    println(result)
}